# oscarcv/homebrew-tap

This repository is a Homebrew tap.

Use it to install packages published in this tap:

```bash
brew tap oscarcv/tap
brew install <formula-name>
```

Or install directly in one command:

```bash
brew install oscarcv/tap/<formula-name>
```

Formula files should live in the `Formula/` directory.
